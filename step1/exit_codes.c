#include <stdio.h>
#include <stdlib.h>     // Requred for EXIT_SUCCESS and EXIT_FAILURE

int main(void) {
    fprintf(stdout,"Executing program logic...\n");
    // standared way to return a success status to the OS
    return EXIT_SUCCESS;
}